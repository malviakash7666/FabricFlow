{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "sequelize": "sequelize-cli",
    "db:create": "npm run sequelize -- db:create",
    "db:drop": "npm run sequelize -- db:drop",
    "db:migrate": "npm run sequelize -- db:migrate",
    "migration:create": "npm run sequelize -- migration:generate --name",
    "db:migrate:undo": "npm run sequelize -- db:migrate:undo",
    "db:migrate:undo:all": "npm run sequelize -- db:migrate:undo:all",
    "db:seed": "npm run sequelize -- db:seed:all",
    "db:seed:undo": "npm run sequelize -- db:seed:undo:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "^1.19.0",
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.22.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.8"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.5"
  }
}
